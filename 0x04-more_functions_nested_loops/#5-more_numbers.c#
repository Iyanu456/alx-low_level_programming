#include "main.h"
/**
 *print_numbers - checks for uppercase
 * Description: checks for uppercase
 */
void more_numbers(void)
{
	int a;

	for (a = '0'; a <= '9'; a++)
	{
		int b;

		for (b = '0'; b <= '14'; b++)
		{
			_putchar (b);
	}
	_putchar ('\n');
}
