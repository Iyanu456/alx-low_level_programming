#include "main.h"
/**
 * _print_numbers - checks for uppercase
 * Description: checks for uppercase
 * Return: 1
 */
int print_numbers(void)
{
	int a;

	for (a = 0; a <= 9; a++)
	{
		_putchar(a);
	}
	_,putchar('\n')
	return (1);
}
