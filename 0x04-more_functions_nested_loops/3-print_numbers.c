#include "main.h"
/**
 *print_numbers - checks for uppercase
 * Description: checks for uppercase
 */
void print_numbers(void)
{
	int a;

	for (a = 0; a <= 9; a++)
	{
		_putchar (a);
	}
	_putchar ('\n');
}
