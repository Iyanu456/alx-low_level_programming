#include "main.h"
#include "string.h"
#include <stdio.h>
/**
 * _strlen_recursion - prints a atring length through recursion
 * @s: a string
 * Description: prints the length a string
 * Return: integer
 */

int _strlen_recursion(char *s)
{
	return (strlen(s));
}
