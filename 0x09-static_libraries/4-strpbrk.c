#include "main.h"
#include <stdio.h>
/**
 * _strpbrk - prints a string
 * @s: string
 * @accept: string
 * Description: prints a string to std output
 * Return: size_t
 */
char *strpbrk(const char *s, const char *accept)
{
}
