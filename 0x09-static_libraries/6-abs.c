#include <stdlib.h>
#include "main.h"
/**
 * _abs - computes absolute values
 * @n: integer
 * Description - computes absolute values
 * Return: integer values
 */
int _abs(int n)
{
	return (abs(n));
}
