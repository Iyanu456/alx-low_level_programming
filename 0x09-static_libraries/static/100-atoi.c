#include "main.h"
#include <stdlib.h>
#include <string.h>
/**
 * _strlen - rests an integer to 98
* @s: integer
 * Description: resets a number to 98
 * Return: integer
 */

int _atoi(char *s)
{
	int b;

	b = atoi(s);

	return (b);
}
