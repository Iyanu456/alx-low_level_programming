#include "main.h"
#include <stdio.h>
/**
 * _strspn - prints a string
 * @s: string
 * @accept: string
 * Description: prints a string to std output
 * Return: size_t
 */

unsigned long int _strspn(const char *s, const char *accept)
{
}
