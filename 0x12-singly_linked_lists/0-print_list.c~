#include "lists.h"

size_t print_list(const list_t *h)
{
	size_t count = 0;

	while (h)
	{
		if (h->str == NULL)
		{
			h->str = "(nil)";
			h->len = 0;
			count++;
		}
		else
			count++;
		printf("[%d] %s", h->len, h->str);
		h = h->next;
	}
	return (count);
}
