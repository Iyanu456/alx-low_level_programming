#include <stdio.h>
/**
 * main - prints all alphabets
 * Description: prints all the alphabets from a to z
 * Return: 0
 */
int main(void)
{
	int x;

	for (x = 0; x <= 9; x++)
	{
	putchar (x);
	}
	putchar ('\n');
	return (0);
}
